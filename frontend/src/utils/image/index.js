import imageLoader from './imageLoader';
import imageExtractor from './imageExtractor';

const image = {
  imageLoader,
  imageExtractor
};

export {
  imageLoader,
  imageExtractor
};

export default image;

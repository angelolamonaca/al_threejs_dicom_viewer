import React, { FC } from 'react';
import './App.css';
import Viewport from './components/Viewport';

const App: FC = () => (
  <div className="App">
    <Viewport />
  </div>
);

export default App;
